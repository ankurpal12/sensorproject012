aiofiles
dill==0.3.5.1
dnspython==2.2.1
database-connect==0.1.66
evidently>=0.4.6
Flask==2.2.3
httptools==0.5.0
imblearn==0.0
ipykernel
jinja2==3.1.2
mypy-boto3-s3==1.24.76
pip-chill==1.0.1
pymongo==4.2.0
python-dotenv==0.21.0
python-multipart==0.0.6
seaborn==0.12.2
types-s3transfer==0.6.0.post4
uvicorn==0.23.2
watchfiles==0.17.0
websockets==10.3
wincertstore==0.2
xgboost==1.6.2
neuro-mf==0.0.5
boto3==1.26.105
Werkzeug==2.2.2

#-e.  
# we use "pip install -r requirements.txt" to install the dependencies from this file. this is used to install and run setup.py file directly from the source code. need not to use python setup.py install command.
# it will install the package in editable mode. It means if we make any changes in the source code then it will reflect in the installed package.
# here we commented it because we don't want to install the package in editable mode every time we add a new dependency. Instead, we will use pip-chill to manage the dependencies.